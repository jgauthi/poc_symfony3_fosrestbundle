{% extends "base.html.twig" %}
{% set title = 'advert.action.add'|trans %}

{% block bodycontent %}
    {{ include("advert/form.html.twig") }}

    <p>
        <a href="{{ path('platform_home') }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            {{ 'page.home.comeback'|trans }}
        </a>
    </p>

{% endblock %}